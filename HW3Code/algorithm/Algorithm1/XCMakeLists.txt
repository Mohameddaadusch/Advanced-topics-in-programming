cmake_minimum_required(VERSION 3.13)

# Define the project for Algorithm 1
project(Algorithm1 VERSION 1.0 LANGUAGES CXX)

# Add the shared library for Algorithm 1
add_library(algo1 SHARED MyAlgorithm.cpp)

# Include directories
target_include_directories(algo1 PRIVATE ${CMAKE_SOURCE_DIR}/../../common)

# Include the common and other necessary directories
target_include_directories(algo1 PUBLIC ${CMAKE_SOURCE_DIR}/../common)
